#include "main.h"

/**
*_isalpha: check if character is alpha
*@c: character to be checked
*
* Return: 1 for alpha and 0 otherwise
*/

int _isalpha(int c)
{
if (c >= 'A' && c <= 'Z')
    Return(1);
else
    Return(0);
}